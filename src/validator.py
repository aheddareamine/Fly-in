# if value <= 0:
        #     raise ParseException("wrong db_drones format: negative or null value")
# in parser: check for nb_drones to be positive

# in parser: parsing start_hub need to check its name isn't in hubs or end hub

# in parse_hub: x and y should be positive

# in parse_options: if max_drones <= 0:
                #     raise ParseException("wrong options format: invalid max_drones value")

